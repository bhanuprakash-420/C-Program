// Online C compiler to run C program online
#include <stdio.h>

int main() 
{
 int a=10,b=10,c=5,result;
 
 result= (a==b) && 
 
 
 (b>c);
 printf("(a==b) && (b>c) is %d\n",result);
 
 result= (a==b) && (b<c);
 printf("(a==b) && (a<b) is %d\n",result);
 
 result= (a==b) ||(a>c);
 printf("(a==b) || (a>c) is %d\n",result);
 
 result= (a==c) || (a<c);
 printf("(a==c) || (a<c) is %d\n",result);
 
 result=!(a!=c);
 printf(" !(a!=c) is %d\n", result);

    return 0;
}
